

## Linear Regression 

### 선형 회귀 

 
데이터들의 입력 값과 출력값 사이의 관계가 선형적이다 라고 가정하고 학습시키는 방법  

Ex) 커피 먹은 횟수와 시험 점수간의 관계 

### 원리 

임의의 함수를 그린다. Ex) `H(x) = Wx + b`

`W` 값과  `b` 값을 바꿔가면서 해당 값의 `Cost`를 확인한다.

`Cost`는 해당 가정이 된 `H(x)`와 실제 데이터들의 거리 값을 의미하고 

해당 `Cost` 중 가장 낮은 가설을 찾아가는 것이다. 
 